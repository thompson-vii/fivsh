<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title or metadata.title }}</title>
    <meta name="description" content="{{ description or metadata.description }}">
    <link rel="stylesheet" href="{{ '/css/index.css' | url }}">
    <link rel="stylesheet" href="{{ '/css/post.css' | url }}">
    <link rel="stylesheet" href="{{ '/css/prism-base16-monokai.dark.css' | url }}">
    <link rel="alternate" href="{{ metadata.feed.path | url }}" type="application/atom+xml" title="{{ metadata.title }}">
    <link rel="alternate" href="{{ metadata.jsonfeed.path | url }}" type="application/json" title="{{ metadata.title }}">
    <link rel="stylesheet" href="{{ '/photoswipe/dist/photoswipe.css' | url }}" media="print" onload="this.media='all'">  
  </head>
  <body>
    <header>
      
      <a href="{{ '/' | url }}" class="home logo">
      <img src="/img/logo.png" alt="Why hire an artist when I can make my own logo?" width="100" height="100">
      </a>

      {#- Read more about `eleventy-navigation` at https://www.11ty.dev/docs/plugins/navigation/ #}
      <ul class="nav">
      {%- for entry in collections.all | eleventyNavigation %}
        <li class="nav-item{% if entry.url == page.url %} nav-item-active{% endif %}"><a href="{{ entry.url | url }}">{{ entry.title }}</a></li>
      {%- endfor %}
      </ul>
      <footer>
            <div class="social-link">
              <object data="/icon/rss.svg" type="image/svg+xml"></object>
              <a href="/feed/feed.xml" target="_blank"> RSS feed </a>
            </div>
           <div class="social-link">
              <object data="/icon/twitter.svg" type="image/svg+xml"></object>
              <a href="https://www.twitter.com/kolloom1" target="_blank"> Twitter </a>
            </div>
           <div class="social-link">
              <object data="/icon/discord.svg" type="image/svg+xml"></object>
              <a href="" target="_blank"> kolloom#4564 </a>
            </div>
           <div class="social-link">
              <object data="/icon/email.svg" type="image/svg+xml"></object>
              <a href="mailto: fivshme@outlook.com" target="_blank"> eMail </a>
            </div>                                    
      </footer>
    </header>

    <main{% if templateClass %} class="{{ templateClass }}"{% endif %}>

      

      {{ content | safe }}
    </main>

    <!-- Current page: {{ page.url | url }} -->
  </body>
</html>
